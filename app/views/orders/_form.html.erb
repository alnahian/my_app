<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :নাম %><br />
    <%= f.text_field :name, :size => 40 %>
  </div>
  <div class="field">
    <%= f.label :ঠিকানা%><br />
    <%= f.text_area :address, :rows => 3, :cols => 40 %>
  </div>

  
  <div class="field">
    <%= f.label :পরিশোধ_করেছেন %><br />
    <%= f.text_field :paid_amount %>
  </div>
  
  <div class="field">
    <%= f.label :পূর্বের_বাকি %><br />
    <%= f.text_field :previous_due %>
  </div>
  
  <div class="field">
    <%= f.label :তারিখ %><br />
    <%= f.datetime_select :published_at %>
  </div>
  
  <div class="actions">
    <%= f.submit 'অর্ডার করুন', class: "btn btn-default btn-warning" %>
  </div>
<% end %>
